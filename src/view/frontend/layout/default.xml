<?xml version="1.0"?>
<!--
    MINIMAL default.xml to avoid Hyvä conflicts

    Removed all block references that cause conflicts with Hyvä Theme.
    The SEO functionality will work through observers only.
-->
<page xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:noNamespaceSchemaLocation="urn:magento:framework:View/Layout/etc/page_configuration.xsd">
    <body>
        <!-- All layout blocks removed to avoid Hyvä Theme conflicts -->
        <!-- SEO meta tags are handled by observers instead -->
    </body>
</page>
