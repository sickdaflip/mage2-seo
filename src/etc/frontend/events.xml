<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="urn:magento:framework:Event/etc/events.xsd">
    <!--
        ALL OBSERVERS TEMPORARILY DISABLED FOR DEBUGGING

        If products/categories work with this file, re-enable observers one by one
        to find which one is causing the issue.
    -->

    <!-- Dummy event with dummy observer to keep XML schema happy -->
    <event name="controller_action_postdispatch">
        <observer name="dummy.observer" instance="FlipDev\Seo\Observer\RemoveMetaKeywords" disabled="true" />
    </event>
</config>